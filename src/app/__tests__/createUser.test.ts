import { createUser } from "../createUser";

describe("Create User Use Case", () => {
  it("should return a boolean success property if user is successfully created", async () => {
    // const mockRequest
    // const mockResponse
    // const mockUserRepo
    // mock UserRepo create method to resolve
    // expect(createUser(mockRequest, mockResponse, mockUserRepo)).toHaveProperty('success', true)
    // expect(createUser(mockRequest, mockResponse, mockUserRepo)).toHaveProperty('status', 201)
  });
  it("should return a boolean success property if user is successfully created", async () => {
    // const mockRequest
    // const mockResponse
    // const mockUserRepo
    // mock UserRepo create method to resolve
    // expect(createUser(mockRequest, mockResponse, mockUserRepo)).toHaveProperty('success', true)
  });
});
